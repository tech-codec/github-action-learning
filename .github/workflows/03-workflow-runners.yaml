name: 03 - Workflow Runners

on: push

jobs:
  ubuntu-echo:
    runs-on: ubuntu-latest

    steps:
      - name: Show OS
        run: echo "This job is running on an Ubuntu runner. ($RUNNER_OS)"

  windows-echo:
    runs-on: windows-latest

    steps:
      - name: Show OS
        shell: pwsh
        run: echo "This job is running on a Windows runner. ($env:RUNNER_OS)"

  mac-echo:
    runs-on: macos-latest

    steps:
      - name: Show OS
        run: echo "This job is running on a MacOS runner. (${{runner.os}})"
